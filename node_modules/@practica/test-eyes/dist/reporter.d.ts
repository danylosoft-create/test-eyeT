import type { FullConfig, FullResult, Reporter, Suite, TestCase, TestResult as PlaywrightTestResult } from '@playwright/test/reporter';
import type { TestEyesReporterOptions } from './types.js';
export declare class TestEyesReporter implements Reporter {
    private options;
    private testAttempts;
    private startTime;
    constructor(options?: TestEyesReporterOptions);
    private validateOptions;
    private getPrNumber;
    private isCI;
    private log;
    private logError;
    onBegin(_config: FullConfig, _suite: Suite): void;
    onTestEnd(test: TestCase, result: PlaywrightTestResult): void;
    onEnd(_result: FullResult): Promise<void>;
    private buildTestResults;
    private mapOutcome;
    private submitData;
    private postComment;
    private buildRunData;
    private getCurrentSha;
}
//# sourceMappingURL=reporter.d.ts.map